generator client {
  provider = "prisma-client-js"
  output   = "../../../../../tmp/prisma-test-client"
}

datasource db {
  provider = "sqlite"
  url      = "file:dev.db"
}

model Test {
  id String @id
}
